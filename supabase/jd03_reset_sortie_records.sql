-- ============================================================================
-- 수동 조종 모드 개편(30초 타임어택 → 연료 서바이벌)에 따른 단판 기록 초기화
-- 두 모드의 기록은 비교가 불가능하므로 공정성을 위해 새 출발한다.
-- 실행: Supabase 대시보드 → SQL Editor (1회성)
-- 클라이언트 쪽은 세이브의 sortieGen 필드로 자동 리셋된다.
-- ============================================================================

update jd03_pets set sortie_best_kg = 0, updated_at = now();
delete from jd03_weekly_sorties;
